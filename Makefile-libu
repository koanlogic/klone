all: party

PARTY_BASE = libu
PARTY_DEP = ${PARTY_BASE}/src/libu.a
PARTY_ARGS = -v --enable_debug --defown=${DEFOWN} --defgrp=${DEFGRP}
PARTY_NO_INSTALL = true

include party.mk
include Makefile.conf
