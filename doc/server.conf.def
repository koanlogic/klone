server.list                  httpd

server.httpd.type            http
server.httpd.addr.type       IPv4
server.httpd.addr.port       4567
server.httpd.dir_root        /www
    # server.httpd.error.404      /embedded/notfound.kl1
server.httpd.server_sig      My Server 0.1




# server.list     httpd
# 
# # this is just a variable that may be used in config values
# #
# # variables can ONLY be local (i.e. in the same {} block) or FULLY scoped 
# # (full path required: ${server.httpd.type)
# #
# shared_dir_root     /var/www/
# 
# server.httpd
# {
#     type            http  
#     addr.type       IPv4
#     addr.port       4567
#     server_sig      My Server 0.1
#     dir_root        /embedded
#     dir_upload      /upload
#     alias           /img    ${dir_root}/graphics/images
#     alias           /boh    ${dir_root}/bohdir
#     virtual_hosts   inquare, codesink, altro
#     inquare
#     {
#         host        www.inquare.com
#         dir_root    ${server.httpd.dir_root}/inquare
#     }
#     codesink
#     {
#         host        www.codesink.org
#         dir_root    ${server.httpd.dir_root}/codesink
#     }
#     altro
#     {
#         host        www.altro.org
#         dir_root    ${shared_dir_root}/altro
#     }
# }

